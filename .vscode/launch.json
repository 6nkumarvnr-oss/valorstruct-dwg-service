{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TET Engine (Uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["tet_engine.app.main:app", "--reload", "--port", "8001"],
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
